# Canonical production origin. Required for a Vercel Production deployment.
NEXT_PUBLIC_SITE_URL=https://kairank.com

# Optional but recommended for reliable Google PageSpeed quota.
PAGESPEED_API_KEY=

# HTTPS endpoint that confirms deeper visibility-review requests.
AUDIT_LEAD_WEBHOOK_URL=

# Optional bearer token used when calling the lead webhook.
AUDIT_LEAD_WEBHOOK_TOKEN=
